import type { Entry } from './types'

export const notes: Entry[] = [
  { 
    slug: 'understanding-tf-idf-and-bm25', 
    title: 'Understanding TF-IDF and BM25', 
    description: 'A practical mental model for how search engines rank text.', 
    date: '08 Sep 2026', 
    body: [
      '**TF** stands for term frequency and **IDF** stands for inverse document frequency. It measures how important a word is to a particular document relative to the entire collection of documents.',
      'Say there is a sentence: "`Redis` is a fast in-memory database."',
      '**TF** is the number of times the word `Redis` appears in the sentence. **IDF** is the inverse of the number of documents that contain the word `Redis`. **TF-IDF** is the product of TF and IDF.',
      'Documents can then be represented as TF-IDF vectors and compared with the query (commonly using **cosine similarity**) to rank the most relevant documents.',
      'The right mental model is',
      'TF-IDF asks:',
      '> **TF** → Is this word important *here*?',
      '> **IDF** → Is this word special *globally*?',
      '> **TF-IDF** → How strongly does this word characterize *this document*?',
      '**BM25** (Best Matching 25) is a search ranking algorithm that can be thought of as a more practical refinement of TF-IDF.',
      'It asks: how relevant is this document to the query?',
      'The core idea: like TF-IDF, BM25 rewards terms that are:',
      '- Rare across documents → higher importance (**IDF**)',
      '- Present frequently in this document → higher relevance',
      '1. Term Frequency Saturation',
      'More occurrences help, but with **diminishing returns**.',
      'Example — `redis` appearing:',
      '- 1 time → strong signal',
      '- 2 times → stronger signal',
      '- 10 times → somewhat stronger',
      '- 100 times → not 100× more relevant',
      'So **BM25** prevents repeated keywords from dominating the score.',
      '2. Document Length Normalization',
      '**BM25** considers the length of the document.',
      'If `redis` occurs 5 times:',
      '- in a 100-word document → probably significant',
      '- in a 10,000-word document → less significant',
      'So it normalizes term frequency based on document length.',
      'The right mental model is',
      'BM25 asks:',
      '> Is the query term rare *globally*?',
      '> Does it occur enough in *this document*?',
      '> Is its frequency meaningful given the document\'s *length*?',
      'So:',
      '**BM25 ≈ IDF + saturated term frequency + document-length normalization**',
      'Compared with TF-IDF:',
      '- **TF-IDF:** frequent locally × rare globally',
      '- **BM25:** frequent locally (with diminishing returns) × rare globally, adjusted for document length',
    ],
  },
]
